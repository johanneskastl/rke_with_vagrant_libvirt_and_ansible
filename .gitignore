#
# Ignore vagrant-related files
#
.vagrant
Vagrantfile.1

# ignore rke's state file that should NOT be put into git (at least not here...)
cluster.rkestate

# ignore the kubeconfig file generated by rke
kube_config_cluster.yml

# ignore the rke cluster.yml created by Ansible
cluster.yml

# ignore the ssh key files
ssh_key_rke
ssh_key_rke.pub
